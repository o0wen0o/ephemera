# 原型记录

问题：一个简约、文艺、略微复古的绿色日记应用，应当以日记书桌、时间长卷，还是打开的写作纸页作为主入口？

位置：新应用的 `/prototype?variant=A|B|C`。无既有项目可以嵌入，因此采用独立原型页面。全部实现保留在 `prototype/ephemera` 分支。

配色：苔绿 `#354e3f`、深林绿 `#2d4335`、鼠尾草 `#a5b397`、纸白 `#f8f8f3`、铅灰绿 `#81877b`。标题和文字书页使用 Noto Serif SC，控件使用系统无衬线体。

版式 A 将收纳、写作与回顾放在一个安静的书桌中；B 让日期统领长篇阅读；C 将写作提到第一层。三者共享数据与编辑器，结构各自独立。

阶段性判断：默认 A，便于第一次使用的人同时理解记录、回顾、珍藏；B/C 保留用于对比。这是设计者的初始选择，尚无用户偏好验证，不宣称它已经是胜出的正式设计。

本次验证所得：本机草稿必须即时写入，延迟写入会在快速关闭时丢失末尾输入；错落日记卡片须随内容独立流动，不能用固定负边距模拟；字体和图片须本地打包以便离线保持书页体验。

后续正式实现应重新整理原型代码，并单独验证云端数据边界、多个设备的冲突解决和隐私保护。当前没有实施议题链接，因此此文件同时作为原型的上下文指针。
